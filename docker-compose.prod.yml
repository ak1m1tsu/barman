services:
  bot:
    image: "ghcr.io/ak1m1tsu/barman:${IMAGE_TAG}"
    volumes:
      - ./configs/config.yaml:/app/configs/config.yaml:ro
      - bot-data:/app/data
    restart: unless-stopped

volumes:
  bot-data:
