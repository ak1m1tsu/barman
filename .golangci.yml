run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/ak1m1tsu/barman

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
        - govet
